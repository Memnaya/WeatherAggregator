name: Node CI

on:
  push:
    branches: "main"
    
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      - name: Make git push
      - run: make install
      - run: make lint
